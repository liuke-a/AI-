# 遗传算法（SGA）核心知识点总结+例题详解
## 一、核心概念
- **个体**：问题的一个可行解，对应搜索空间中的一个点。
- **种群**：若干个体组成的集合，是算法的操作对象（规模固定）。
- **染色体**：个体的二进制串编码（SGA默认），对应可行解的符号表示。
- **基因**：染色体上的单个位（0或1），对应可行解的参数片段。
- **适应度函数**：评价个体优劣的核心函数，需将目标函数转换为“越大越优”（最小化问题需映射，如$Fitness=C-f(x)$）。
- **遗传操作**：选择-复制、交叉、变异（对应生物进化的自然选择、基因重组、基因突变）。

## 二、核心流程
1. **参数设定**：确定种群规模$N$、交叉率$P_c$（0.4，0.99）、变异率$P_m$（0.0001，0.1）、最大代数$T$。
2. **编码与初始化**：二进制编码生成$N$个合法个体（随机生成0/1串，规避非法解），组成初始种群。
3. **适应度计算**：解码个体得到可行解，代入适应度函数，计算每个个体的适应度值。
4. **遗传操作**：
   - 选择-复制：按适应度概率复制$N$次，生成规模为$N$的中间种群。
   - 交叉：按$P_c$选择个体配对，随机选交叉点交换基因，替换原个体。
   - 变异：按$P_m$随机选择基因位翻转，修正非法解。
5. **终止判断**：达到最大代数或种群收敛，输出适应度最高的个体（最优解）。

## 三、关键参数与操作规则
### 1. 核心参数
| 参数         | 作用                  | 经验取值       |
|--------------|-----------------------|----------------|
| 种群规模$N$ | 保证多样性与计算效率  | 20~100         |
| 交叉率$P_c$ | 控制基因重组频率      | 0.6~0.9        |
| 变异率$P_m$ | 控制基因突变频率      | 0.001~0.01     |
| 最大代数$T$ | 算法终止阈值          | 100~500        |

### 2. 遗传操作细节
- **选择-复制**：轮盘赌法，选择概率$P(x_i)=\frac{Fitness(x_i)}{\sum Fitness(x_j)}$，累计概率匹配随机数选择。
- **交叉**：
  - 参加交叉的染色体数$c=N×P_c$（修正为偶数），交叉次数$c$。
  - 随机打乱个体顺序两两配对，随机选1个交叉点（1~染色体长度-1）。
- **变异**：
  - 变异次数$m=总基因数×P_m$（总基因数$=N×$染色体长度）。
  - 逐位生成随机数，小于$P_m$则翻转基因，非法解需修正。

## 四、例题：手工计算两轮迭代（一元函数最大化）
### 问题
求$f(x)=x^2$在$x∈[0,31]$的最大值，用遗传算法手工计算2轮迭代。

### 步骤1：参数设定
- 种群规模$N=4$，染色体长度=5（覆盖0~31），$P_c=1.0$（全交叉），$P_m=0.001$，最大代数$T=4$。
- 适应度函数：$Fitness(x)=x^2$（最大化问题直接用目标函数）。

### 步骤2：初始种群（第0代）
随机生成4个5位二进制个体，解码后如下：
| 个体编号 | 染色体 | 解码$x$ | 适应度$x^2$ | 选择概率 | 累计概率 |
|----------|--------|-----------|---------------|----------|----------|
| 1        | 01101  | 13        | 169           | 0.14     | 0.14     |
| 2        | 11000  | 24        | 576           | 0.49     | 0.63     |
| 3        | 01000  | 8         | 64            | 0.06     | 0.69     |
| 4        | 10011  | 19        | 361           | 0.31     | 1.00     |

### 步骤3：第1轮迭代
#### （1）选择-复制（生成中间种群$S_1$）
模拟4个随机数：0.45、0.11、0.57、0.98，匹配累计概率选择：
- 0.11≤0.14→选个体1；0.14<0.45≤0.63→选个体2；0.63<0.57≤0.69→选个体2；0.69<0.98≤1.00→选个体4。
- 复制后$S_1$：[11000（24）、01101（13）、11000（24）、10011（19）]。

#### （2）交叉操作
- 参加交叉数$c=4×1.0=4$（偶数），交叉次数=2次。
- 随机配对：（11000, 01101）、（11000, 10011），交叉点选第3位（合法范围1~4）。
  - 第1对：110|00 × 011|01 → 交叉后11001（25）、01100（12）。
  - 第2对：110|00 × 100|11 → 交叉后11011（27）、10000（16）。
- 交叉后$S_2$：[11001（25）、01100（12）、11011（27）、10000（16）]。

#### （3）变异操作
- 总基因数=4×5=20，变异次数=20×0.001=0.02（不足1次，不变异）。
- 第1轮迭代后种群（第1代）：

| 个体编号 | 染色体 | 解码$x$ | 适应度$x^2$ |
|----------|--------|-----------|---------------|
| 1        | 11001  | 25        | 625           |
| 2        | 01100  | 12        | 144           |
| 3        | 11011  | 27        |729           |
| 4        | 10000  | 16        | 256           |


### 步骤4：第2轮迭代
#### （1）适应度与选择-复制
| 个体编号 | 适应度 | 选择概率 | 累计概率 |
|----------|--------|----------|----------|
| 1        | 625    | 0.36     | 0.36     |
| 2        | 144    | 0.08     | 0.44     |
| 3        | 729    | 0.41     | 0.85     |
| 4        | 256    | 0.15     | 1.00     |

- 随机数：0.30、0.70、0.50、0.90，选择结果：个体1、3、3、4。
- 复制后$S_1$：[11001（25）、11011（27）、11011（27）、10000（16）]。

#### （2）交叉操作
- 配对：（11001, 11011）、（11011, 10000），交叉点选第4位。
  - 第1对：1100|1 × 1101|1 → 交叉后11001（25）、11011（27）。
  - 第2对：1101|1 × 1000|0 → 交叉后11010（26）、10001（17）。
- 交叉后$S_2$：[11001（25）、11011（27）、11010（26）、10001（17）]。

#### （3）变异操作
- 变异次数=20×0.001=0.02（不变异）。
- 第2轮迭代后种群（第2代）：适应度最高为729（$x=27$），逐步逼近全局最优$x=31$（适应度961）。

### 最终结论
两轮迭代后，最优个体为$x=27$（染色体11011），适应度729，算法通过遗传操作逐步向全局最优收敛。


# 遗传算法的收敛性分析与改进途径
核心结论：遗传算法的收敛性是“以概率找到全局最优解”，受种群规模、遗传操作、参数设置等因素影响；改进的核心方向是“平衡搜索效率与多样性”，避免早熟收敛或搜索停滞。

## 一、收敛性分析
### 1. 收敛的定义
遗传算法的收敛指“经过有限代迭代后，算法能以概率1逼近全局最优解”，需满足两个条件：
- 任意初始种群经有限步能到达全局最优解；
- 存在保优操作（如保留最优个体），避免最优解遗失。

### 2. 影响收敛性的关键因素（考试高频）
#### （1）种群规模
- 种群太小：采样点不足，易陷入局部最优（早熟收敛）；
- 种群太大：计算量剧增，收敛速度缓慢；
- 经验值：种群规模20~100，需结合问题复杂度调整。

#### （2）选择操作
- 正向作用：高适应度个体被选中概率高，提升全局收敛性；
- 保优策略：将父代最优个体直接保留到下一代（不参与交叉/变异），可使算法以概率1收敛；
- 反向作用：选择压力过大（优劣个体选中概率差距悬殊），易导致种群同质化，早熟收敛。

#### （3）交叉概率（$P_c$）
- $P_c$ 太大：优秀个体的基因结构易被破坏，收敛不稳定；
- $P_c$ 太小：基因重组频率低，搜索停滞，算法不收敛；
- 经验值：0.4~0.99，常用0.6~0.9。

#### （4）突变概率（$P_m$）
- $P_m$ 太小：难以产生新基因模式，无法跳出局部最优；
- $P_m$ 太大：算法退化为随机搜索，失去进化意义；
- 经验值：0.0001~0.1，常用0.001~0.01。

## 二、改进途径
针对收敛性问题，主要有6大改进方向，核心是“优化编码、调节适应度、改进算子、自适应参数”：

### 1. 编码方式改进
- 二进制编码缺陷：高维问题编码过长，计算效率低；
- 替代编码（考试常考）：
  - 顺序编码：适用于旅行商（TSP）等排序类问题；
  - 实数编码：直接用实数表示基因，无需解码，适用于连续参数优化；
  - 整数编码：针对离散整数解空间（如$x \in \{1,2,...,7\}$类问题）。

### 2. 适值函数改进
核心是“标定”（目标函数→适值函数的映射），目的是调节选择压力：
- 标定目的：
  - 将最小化问题转换为最大化问题；
  - 平衡选择压力，避免早熟收敛或搜索停滞；
- 常用标定方法（考试高频）：
  - 线性标定：$F(x) = a \cdot f(x) + b$（$a$、$b$为常数，保证适应度为正）；
  - 动态标定：随迭代次数调整标定参数，适配种群进化状态；
  - 窗口技术：仅考虑近期迭代的最优适应度，避免早期优势个体垄断选择。

### 3. 遗传算子改进
#### （1）选择算子改进：排序选择
- 操作逻辑：先按适应度降序排序个体，再按固定概率分配选中机会（而非适应度比例）；
- 优势：降低选择压力，避免个体适应度悬殊导致的早熟收敛。

#### （2）交叉算子改进：均匀交叉
- 操作逻辑：生成二进制屏蔽字，按屏蔽字随机决定每位基因是否交换（而非固定交叉点）；
- 优势：基因重组更灵活，增强种群多样性。

#### （3）突变算子改进：逆序突变
- 操作逻辑：对染色体的连续片段进行逆序排列（而非单个基因翻转）；
- 适用于：排序类问题（如TSP），保留优良子路径。

### 4. 控制参数改进
核心是“自适应参数策略”：
- 自适应逻辑：
  - 种群适应度趋于一致（早熟收敛迹象）：增大$P_c$和$P_m$，增加多样性；
  - 种群适应度分散：减小$P_c$和$P_m$，保留优良基因；
  - 高适应度个体：对应较低的$P_c$和$P_m$，保护优良基因；
  - 低适应度个体：对应较高的$P_c$和$P_m$，促进基因重组。

### 5. 执行策略改进
融合其他算法优势，提升收敛性能：
- 混合遗传算法：结合梯度下降等局部优化算法，兼顾全局搜索与局部精细搜索；
- 免疫遗传算法：引入“免疫记忆”“抗体浓度调节”，避免种群同质化；
- 小生境遗传算法：模拟自然界小生境，保留多种优良基因模式，适用于多目标优化。

### 6. 停止准则改进
避免“固定最大代数”的盲目性：
- 观察指标：种群平均适应度（$\bar{F}$）与最大适应度（$F_{max}$）的比值；
- 收敛判定：当$\bar{F}/F_{max}$趋近于1时，说明种群趋于稳定，算法收敛，可提前终止。